const input = require('fs').readFileSync('stdin', 'utf8');
const lines = input.split('\n');

const I = Number(lines[0]);

let str = "";

for (let i = 1; i <= I; i++) {
    str += "a" 
}

console.log(`Feliz nat${str}l!`);